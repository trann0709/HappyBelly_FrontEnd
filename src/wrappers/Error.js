import styled from 'styled-components';

const Wrapper = styled.main`
  display: grid;
  place-items: center;
  text-align: center;
  div {
    padding-top: 5rem;
  }
  .error-photo {
    width: 90vw;
    max-width: 600px;
  }
  h2 {
    margin-top: 1rem;
  }
  .btn {
    margin-top: 1rem;
  }
`;

export default Wrapper;
